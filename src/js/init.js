/*
    Initiates AOS(Animate On Scroll)
    Also contains my overall config
*/
AOS.init();
    AOS.init({
    debounceDelay: 50,
    once: false,
    mirror: true,
    duration: 800
});

export default AOS;
